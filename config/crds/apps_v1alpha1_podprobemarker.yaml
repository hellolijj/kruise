apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: podprobemarkers.apps.kruise.io
spec:
  additionalPrinterColumns:
    - JSONPath: .status.matched
      description: The matched number of pods. This is typically equal to the number
        of nodes satisfied to run pods.
      name: Matched
      type: integer
    - JSONPath: .status.succeeded
      description: The number of probe succeed pods.
      name: Succeeded
      type: integer
    - JSONPath: .status.failed
      description: The number of probe failed pods.
      name: Failed
      type: integer
    - JSONPath: .metadata.creationTimestamp
      description: CreationTimestamp is a timestamp representing the server time when
        this object was created. It is not guaranteed to be set in happens-before order
        across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC.
      name: AGE
      type: date
  group: apps.kruise.io
  names:
    kind: PodProbeMarker
    plural: podprobemarkers
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            selector:
              description: selector is a label query over pods that should be injected
              type: object
            labels:
              type: object
            markProbe:
              properties:
                initialDelaySeconds:
                  format: int32
                  type: integer
                timeoutSeconds:
                  format: int32
                  type: integer
                periodSeconds:
                  format: int32
                  type: integer
                successThreshold:
                  format: int32
                  type: integer
                failureThreshold:
                  format: int32
                  type: integer
                handler:
                  properties:
                    exec:
                      type: object
                    httpGet:
                      type: object
        status:
          type: object
  version: v1alpha1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
